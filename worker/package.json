{
  "name": "cian-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply realty --local",
    "migrate:remote": "wrangler d1 migrations apply realty --remote",
    "test": "node --test --test-concurrency=1 test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
